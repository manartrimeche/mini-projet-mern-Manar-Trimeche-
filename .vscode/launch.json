{
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Backend (npm start)",
            "runtimeExecutable": "npm",
            "runtimeArgs": [
                "start"
            ],
            "cwd": "${workspaceFolder}/backend",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}